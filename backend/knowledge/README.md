# Knowledge pack

Per-tenant product documentation searched by the gateway's `docs_search`
tool. Drop markdown files here (future: the onboarding scrape pipeline
writes them). Empty = the tool reports 'no knowledge pack loaded'.
